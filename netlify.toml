# Netlify configuration for superheld.app documentation
# Build root is the Astro + Starlight site in `astro-site/`.

[build]
  base = "astro-site"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
